Game = {
    
    start: function() {
        Crafty.init(window.innerWidth, window.innerHeight);
        Crafty.canvas.init();
        
        Crafty.background("url('assessts/background.png')");
    
    
}